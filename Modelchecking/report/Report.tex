\documentclass[11pt,a4paper]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[hidelinks]{hyperref}

\newcommand{\always}{\ensuremath{\Box}}
\newcommand{\eventually}{\ensuremath{\Diamond}}
\newcommand{\code}[1]{\texttt{#1}}
\newcommand{\todo}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}

\lstdefinelanguage{Promela}{
  morekeywords={active, proctype, init, chan, byte, bit, bool, short,
    int, mtype, pid, typedef, if, fi, do, od, break, goto, atomic,
    d_step, inline, ltl, never, assert, printf, skip, run, len, empty,
    nempty, full, nfull, timeout, else, of, select, for, in, unless,
    end, progress, accept},
  sensitive=true,
  morecomment=[l]{//}, morecomment=[s]{/*}{*/}, morestring=[b]"
}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\bfseries,
  commentstyle=\itshape\color{gray},
  breaklines=true, showstringspaces=false,
  frame=single, framesep=5pt, xleftmargin=10pt, tabsize=2
}
\lstdefinestyle{promela}{language=Promela}
\lstdefinestyle{clang}{language=C}

\title{Model Checking Concurrent Programs with SPIN\\
       \large RW796 Software Verification and Analysis}
\author{Gabriella Scott \\ 25935453}
\date{\today}

\begin{document}
\maketitle

\section*{Introduction}

This report covers Promela models of two concurrent programs and the
verification of their correctness properties using Spin. The first
program is a first-come first-served (FCFS) process scheduler, one of
three policies supported by a shared-memory process manager. A team of
OpenMP threads dispatches processes from a common ready queue, moves
them to a waiting queue when a resource is unavailable, and updates a
shared resource table, with nested locks protecting these structures
and new processes arriving while the threads are already running. The
second program is a distributed Othello engine running a parallel
alpha-beta search. An MPI master hands candidate moves out to worker
processes as they report back, propagates a shared alpha bound between
them, and reduces the search depth partway through a round when the
turn clock runs down. It communicates over three channels: MPI
collectives to start a round, point-to-point messages to distribute and
collect work, and a socket to the referee.

Parts of each program were omitted, some behaviour was replaced with
nondeterministic choices, and the remaining parameters were fixed to small
values. These three moves do not affect the results in the same way, so it
helps to separate them with the usual pair of terms. An abstraction is
\emph{sound} for a property if every counterexample it reports corresponds to
a real execution of the implementation, meaning there are no false alarms. It
is \emph{complete} if every violation the implementation can commit also
appears in the model, meaning there are no missed defects.

Replacing concrete behaviour with a nondeterministic choice adds executions
rather than removing them, which is why Promela treats nondeterminism as the
standard way to build an abstraction \cite{inggs_promela}. That direction
keeps completeness but puts soundness at risk, because the model can now do
things the implementation never would. Fixing the parameters to small values
and leaving components out works the other way: it removes executions, so a
counterexample that survives is still a real one, but a property that passes
has only been shown to pass inside the bound. Neither model here is purely one
or the other, so both directions are handled explicitly. Every counterexample
is read back against the C source before it is called a defect, and every
passing result is reported with its configuration attached rather than as a
general claim. Each abstraction is justified where it is introduced, and
Section~\ref{sec:limits-1} and Section~\ref{sec:limits-2} give the specific
limits for each program.

The analysis found defects in both programs. For the scheduler, 6 of the 
7 checks failed, with the counterexamples tracing back to two faults in 
\code{manager.c}: a resource request that does not check whether the requester 
already owns the resource, and a dispatch loop that accesses shared process 
state without holding a lock. For the Othello engine, 4 of the 5 checks 
failed, with 3 faults identified in \code{my\_player.c}, namely an initialiser 
escaping from the minimax minimising branch as if it were a score, comparisons 
between scores calculated at different search depths, and two referee message 
types for which the master loop has no corresponding branch. The last fault 
did not occur in any recorded match, illustrating the value of model checking 
in finding behaviours that testing may not expose.

\medskip
\noindent
Promela models, properties and verification records:
\url{https://github.com/Gabriella-Scott/PA_Assignments}\\
Implementation under analysis (Program 1 \& 2): 
\url{https://git.cs.sun.ac.za/Computer-Science/rw314/2025/project/25935453-rw314-projects}\\

% =====================================================================
\section{Program 1: FCFS Process Scheduler}

% ---------------------------------------------------------------------
\subsection{The Promela Model}

The model abstracts the FCFS path of \code{manager.c}: \code{schedule\_fcfs}
together with \code{execute\_instr}, \code{request\_resource},
\code{release\_resource}, \code{enqueue\_pcb}, \code{dequeue\_pcb} and
\code{terminate}. It consists of an \code{init} process that seeds the ready
queue, three inlines standing for \code{terminate}, \code{request\_resource}
and \code{release\_resource}, and a \code{Worker} proctype instantiated once
per scheduling thread. The other two policies are not modelled: all three sit
on the same queues, locks and resource table, so including them would have
multiplied the state space without reaching a different class of defect.

Four constants fix the size of the model, each the smallest value that still
shows the behaviour of interest. \code{NUM\_WORKERS} is 2, because a single
thread cannot interleave with anything. \code{NUM\_PROCS} is 3, because with 2
processes one running and one blocked leaves the ready queue empty, and a
third is needed before a process can be dispatched while another is already
blocked. \code{NUM\_RESOURCES} is 2, the smallest number admitting a circular
wait \cite{baier2008}. \code{MAX\_INSTR} is 3, enough for a process to
request a resource, release it and request again, so that a release can wake a
blocked process. Identifiers start at 1 so that 0 can stand for \code{NULL}
and \code{resource\_owner[r] == 0} reads exactly as
\code{resource->allocated == NULL} does. Only \code{MAX\_INSTR} is wrapped in
an \code{\#ifndef} guard, since it is the one parameter varied between runs;
supplying it with \code{spin -D} means every result in
Section~\ref{sec:results-1} comes from the same committed file.
Table~\ref{tab:mapping} sets out the correspondence between the
implementation and the model.

\begin{table}[htbp]
\centering
\caption{Mapping between the implementation and the model.}
\label{tab:mapping}
\begin{tabular}{p{3.2cm} p{3.4cm} p{7.2cm}}
\toprule
\textbf{\code{manager.c}} & \textbf{Model} & \textbf{Reason} \\
\midrule
Thread in \code{schedule\_fcfs} &
\code{active [NUM\_WORKERS] proctype Worker} &
The parallel region runs identical threads over the same loop, so one
proctype replicated twice covers every thread the scheduler creates. \\
\addlinespace
\code{readyq}, \code{waitingq} &
Buffered channels \code{ready\_q}, \code{waiting\_q} &
FIFO order is the only behaviour of the linked lists FCFS depends on.
\code{waiting\_q} also carries the resource id, which the implementation
reads from the instruction record. \\
\addlinespace
\code{terminatedq} &
\code{pcb\_state[] == TERMINATED} &
Nothing is ever dequeued from it, so membership carries no information the
state field does not already hold. \\
\addlinespace
\code{pcb\_t.state} &
\code{pcb\_state[]} &
Direct, indexed by process id instead of reached through a pointer. \\
\addlinespace
\code{next\_instruction} &
\code{instr\_count[]} &
The instruction list becomes a bounded counter, with
\code{next\_instruction == NULL} represented by
\code{instr\_count == MAX\_INSTR}. \\
\addlinespace
\code{resource\_t.allocated} &
\code{resource\_owner[]} &
The list searched by \code{strcmp} becomes an array indexed by id, and
\code{allocated == NULL} becomes owner 0. \\
\addlinespace
\code{q\_locks[0]}, \code{omp critical}, \code{i\_lock}, \code{r\_lock} &
\code{atomic} blocks &
Mutual exclusion is all the locks provide here, and \code{atomic} supplies it
without modelling acquisition \cite{spin_atomic}. Only the regions the
implementation actually protects are made atomic. \\
\addlinespace
\code{terminate()} &
\code{check\_terminate} inline &
The same test, evaluated over the state array rather than by walking the
queues. \\
\bottomrule
\end{tabular}
\end{table}

OpenMP nested locks were used to protect the shared structure in the implementation,
which let a thread reacquire a lock it already holds \cite{openmp52};
\code{schedule\_fcfs} takes \code{i\_lock} and then calls
\code{execute\_instr}, which takes it again. The model represents no locks at
all, only the \code{atomic} blocks standing for the regions they protect. That
is sound provided the locks cannot deadlock against each other, which is
checkable by inspection: every FCFS path acquires them in the same order, and
\code{release\_resource}, the only function holding two queue locks at once,
always takes \code{q\_locks[1]} before \code{q\_locks[0]}. With a consistent
aacquisition order no circular wait between locks is possible: a thread
that holds one lock only ever waits for a lock later in the fixed order,
so the wait-for relation cannot contain a cycle \cite{baier2008}. One caveat to note is that a blocking statement inside an
\code{atomic} sequence causes Spin to drop atomicity rather than report an
error \cite{spin_atomic}, so the sends on \code{ready\_q} and
\code{waiting\_q} could in principle interleave, but both channels are sized
at \code{NUM\_PROCS} and can only fill if a PCB is enqueued twice, which is
what \code{no\_dup\_ready} exists to rule out.

What the model leaves outside an \code{atomic} block matters more. The
assignment \code{running\_p->state = RUNNING}, the loop guard
\code{while (running\_p->state == RUNNING)} and the test
\code{if (running\_p->state == WAITING)} all touch shared PCB state with no
lock held, since \code{i\_lock} is released as soon as \code{execute\_instr}
returns. Those statements sit outside any atomic block in the \code{Worker}
loop, so \code{pan} is free to interleave the second worker there. Wrapping
them in \code{atomic} for tidiness would have left the model unable to find
the defect in Section~\ref{sec:results-1}.

\begin{lstlisting}[style=clang,caption={Unprotected reads in \code{schedule\_fcfs}, abridged}]
running_p->state = RUNNING; /* no lock held */
while (running_p->state == RUNNING) /* no lock held */
{
  ...
  omp_set_nest_lock(&i_lock);
  ...
  execute_instr(running_p);
  omp_unset_nest_lock(&i_lock); /* lock released here */

  load_new_processes();

  if (running_p->state == WAITING) /* no lock held */
  {
    break;
  }
  running_p->next_instruction = running_p->next_instruction->next;
  ...
}
\end{lstlisting}

Two arrays in the model do not have direct counterparts in \code{manager.c};
\code{executing[]} keeps track of how many workers are currently processing a 
PCB, while \code{in\_ready[]} tracks how many times a PCB appears in the ready 
queue. Since neither is used in a guard, they cannot enable or block any 
transitions; only provide the state needed by \code{pcb\_mutex} and 
\code{no\_dup\_ready}.

The instruction stream is where the model makes the biggest abstraction, because
the implementation reads instructions from a linked list created by the loader, 
where each instruction has a type and resource name. The model does not 
include these instruction records; instead, \code{select} non-deterministically 
chooses a resource, while a two-branch \code{if} non-deterministically 
chooses between a request and a release \cite{inggs_promela}. 
Because \code{pan} explores both branches, the model covers every possible 
instruction sequence of up to \code{MAX\_INSTR} steps, including sequences that 
the actual loader would not produce.
Table~\ref{tab:omissions} lists what was left out and what each omission
costs.

\begin{table}[htbp]
\centering
\caption{What the model leaves out, and what each omission costs.}
\label{tab:omissions}
\begin{tabular}{p{4.2cm} p{9.8cm}}
\toprule
\textbf{Omitted} & \textbf{Cost} \\
\midrule
Process names, \code{strcmp} lookup &
None $\rightarrow$ Resources are found by index (instead of by walking a list). \\
\addlinespace
Logging &
None $\rightarrow$ It writes to a file and touches no scheduling state. \\
\addlinespace
\code{omp barrier}, \code{omp single} &
None $\rightarrow$ They run after every process has terminated, so no property here can
distinguish them. \\
\addlinespace
The "resource does not exist" path &
Small but real $\rightarrow$ The implementation reaches the same
\code{enqueue\_pcb(..., WAITING)} branch whether a resource is held by
another process or absent entirely; the model represents only the first. \\
\addlinespace
Dynamic arrivals and \code{a\_lock} &
Not free $\rightarrow$ \code{load\_new\_processes} can add to the ready queue while
\code{terminate()} is counting it, so omitting arrivals removes a race the
model cannot then find. \\
\addlinespace
\code{detect\_deadlock} &
Not free $\rightarrow$ Deadlock is checked by Spin's invalid end state detection instead,
which asks whether the system halted where it should not have rather than
whether a circular wait exists. \\
\bottomrule
\end{tabular}
\end{table}

Three changes were needed because of how Promela works. An \code{if} 
statement with no executable guard blocks, so the test after an instruction
uses two explicit guards for \code{WAITING} instead of a guard and an 
\code{else}. Spin also doesn't allow \code{else} to be used with channel 
operations, so the dequeue uses \code{nempty(ready\_q)} and 
\code{empty(ready\_q)} as two explicit cases.

A worker can try to dequeue a process before the ready queue has been filled,
because the \code{active} proctypes start at the same time as \code{init}. 
The \code{empty(ready\_q)} branch handles this case, but it also 
causes the simple liveness failures discussed in Section~\ref{sec:results-1}.

\subsubsection*{Limits on interpretation}
\label{sec:limits-1}

The model was written by hand, so a counterexample is treated as a possible 
defect until its interleaving has been checked against the source code. 
This is done for each finding in Section~\ref{sec:results-1}. On the other 
hand, a property that holds only applies to the model configuration of 3 
processes, 2 workers, 2 resources, and \code{MAX\_INSTR} steps. One important 
assumption is that the model treats unprotected reads of 
\code{running\_p->state} as ordinary reads that see the most recent value 
written. OpenMP does not guarantee this behaviour without a 
\code{flush} \cite{openmp52}. Consequently, the real program may have behaviours 
that are not represented in the model. This means that the identified defects are a 
lower bound on the problems with the unlocked dispatch loop.

% ---------------------------------------------------------------------
\subsection{Correctness Properties}
\label{sec:props-1}

Seven checks were run: 6 properties written as named \code{ltl} blocks in
\code{model.pml}, plus a deadlock check that is not an LTL property.
Table~\ref{tab:props} gives each formula as it appears in the file, where
\code{[]} is $\always$ and \code{<>} is $\eventually$ \cite{inggs_properties}. 4 are safety
properties, which say a bad state is never reached and are violated by a
finite prefix, and 2 are liveness properties, which say something good
eventually happens and can only be violated by an infinite execution
\cite{inggs_properties}. Promela has no quantifier, so every conjunction over
the three process ids is written out by hand.

\begin{table}[htbp]
\centering
\caption{The six LTL properties as written in \code{model.pml}.}
\label{tab:props}
\begin{tabular}{l l p{9.4cm}}
\toprule
\textbf{Property} & \textbf{Class} & \textbf{Formula} \\
\midrule
\code{pcb\_mutex} & safety &
\code{[] (executing[p] <= 1)} for each \code{p} \\
\addlinespace
\code{no\_self\_wait} & safety &
\code{[] (waiting\_for[p] == 0 || resource\_owner[waiting\_for[p]] != p)} \\
\addlinespace
\code{waiting\_consistent} & safety &
\code{[] (pcb\_state[p] != WAITING || waiting\_for[p] != 0)} \\
\addlinespace
\code{no\_dup\_ready} & safety &
\code{[] (in\_ready[p] <= 1)} \\
\addlinespace
\code{all\_terminate} & liveness &
\code{<> (pcb\_state[1] == TERMINATED \&\& ... \&\& pcb\_state[3] == TERMINATED)} \\
\addlinespace
\code{ready\_dispatched} & liveness &
\code{[] ((pcb\_state[1] == READY) -> <> (pcb\_state[1] == RUNNING))} \\
\bottomrule
\end{tabular}
\end{table}

\noindent
\textbf{\code{pcb\_mutex}} is an important invariant because the PCB 
represents a unit of work, so only one thread should process it at a time. 
The dequeue operation is protected, but the code after it is not. This means 
two threads could enter the instruction loop for the same PCB, causing both 
to update \code{next\_instruction} and \code{state}.

\medskip\noindent
\textbf{\code{no\_self\_wait}} checks the behaviour of \code{request\_resource}. 
The function only checks whether \code{resource->allocated == NULL} and does 
not check whether the resource is already owned by the requesting process. 
This results in a process being able to request a resource it already holds 
and be placed on \code{waitingq}. Since the process is the one holding the 
resource, it cannot release it while waiting. Unlike the other safety 
properties, this failure does not depend on a particular thread interleaving
(so it is considered separately).

\medskip\noindent
\textbf{\code{waiting\_consistent}} checks that a PCB in state 
\code{WAITING} is actually waiting for a resource, since the state and resource
information are updated separately without a lock protecting both operations. 
This means a PCB can enter the \code{WAITING} state without having a resource 
recorded. It can then remain on the waiting queue without anything being able 
to wake it. This property is considered together with \code{pcb\_mutex}, 
because it shows how overlapping access can lead to incorrect state that 
affects later decisions.

\medskip\noindent
\textbf{\code{no\_dup\_ready}} checks the three places that add processes 
to the ready queue: the long-term scheduler, \code{load\_new\_processes}, 
and \code{release\_resource}. A duplicate entry could cause the same process 
to be dispatched twice and lead to another \code{pcb\_mutex} violation that 
cannot be prevented just by locking the dispatch loop.

\medskip\noindent

\textbf{\code{all\_terminate}} is checked separately from the program's own 
stopping condition because they don't ask the same question. 
\code{terminate()} returns TRUE when all processes have been placed in either 
the waiting or terminated queues. This means a process that is blocked 
forever can still allow the workers to exit and the scheduler to report that 
it has finished.

\medskip\noindent

\textbf{\code{ready\_dispatched}} checks whether the system actually serves 
processes before stopping. It is only checked for process 1 because the 3 
processes are symmetric. FCFS does not use a priority field, and all 3 
instruction sequences are generated using the same nondeterministic choice. 
Therefore, checking one process represents the others and keeps the never 
claim smaller. This is an assumption based on symmetry rather than a formal 
proof, and it is the one property in Program 1 that was weakened to reduce 
the verification cost. Both liveness properties are checked under weak 
fairness, as discussed in Section~\ref{sec:results-1}.

\medskip\noindent
\textbf{Deadlock} is checked with Spin's invalid end state detection, which
asks whether any process is stopped somewhere other than a valid halting
point \cite{inggs_properties}.This matches the standard definition of
deadlock as a state in which the system as a whole has halted while at
least one component could still continue \cite{baier2008}, and is broader
than asking whether a circular wait exists; it catches a process blocked
on a guard that can never become executable, whatever the reason. Since \code{pan}
disables the check while a never claim is active, this run uses a separate
claim-free verifier \cite{spin_pan}.

\medskip\noindent
Three things were deliberately not checked, namely an assertion bounding
\code{instr\_count} was removed, because the deadlock trail already shows the
same overrun and an active assertion would let other runs stop on it rather
than on their named property. \code{detect\_deadlock} is not verified, because
it runs inside \code{\#pragma omp single} and contains no concurrency. Lock
reentrancy is not modelled, because no property depends on nesting depth,
only on which accesses are protected.

% ---------------------------------------------------------------------
\subsection{Verification Results and Analysis}
\label{sec:results-1}

Each run selects a specific claim using \code{./pan -a -N name}. If \code{-N} 
is not provided, \code{pan} uses the first claim in the file by default. 
The deadlock check uses a second binary compiled from the same generated 
\code{pan.c} with \code{-DNOCLAIM}. This removes the claims from the verifier,
rather than from the model itself \cite{spin_pan}. The value of 
\code{MAX\_INSTR} is also supplied to \code{spin} on the command line, 
ensuring that all results in Table~\ref{tab:results} use the same source file.

Two extra flags are used on the larger runs. \code{-m} increases the maximum 
search depth from the default of 10,000, while \code{-w26} increases the hash 
table size to $2^{26}$ entries. The larger hash table was needed because the 
default $2^{19}$ entries caused many hash conflicts at this scale 
\cite{spin_pan}. All runs were performed from the command line using 
Spin 6.5.2 on a machine with 7\,GB of RAM. iSpin was not used.


\begin{table}[htbp]
\centering
\caption{Verification results. Depths and state counts as
reported by \code{pan}.}
\label{tab:results}
\begin{tabular}{l l l r r}
\toprule
\textbf{Property} & \textbf{Config} & \textbf{Result} & \textbf{Depth} & \textbf{States} \\
\midrule
\code{pcb\_mutex} & \code{MAX\_INSTR 3} & violated & 901  & 65\,957 \\
\code{no\_self\_wait} & \code{MAX\_INSTR 3} & violated & 379  & 188 \\
\code{waiting\_consistent} & \code{MAX\_INSTR 3} & violated & 962  & 69\,699 \\
\code{no\_dup\_ready} & \code{MAX\_INSTR 2} & holds    & n/a  & 20\,861\,451 \\
\code{all\_terminate} & \code{MAX\_INSTR 3}, no \code{-f} & violated & 14 & 25 \\
\code{all\_terminate} & \code{MAX\_INSTR 3}, \code{-f} & violated & 1069 & 493 \\
\code{ready\_dispatched} & \code{MAX\_INSTR 3}, no \code{-f} & violated & 14 & 25 \\
\code{ready\_dispatched} & \code{MAX\_INSTR 3}, \code{-f} & violated & 1088 & 11\,372\,991 \\
deadlock & \code{MAX\_INSTR 3}, \code{-DNOCLAIM} & violated & 504 & 67\,872 \\
\bottomrule
\end{tabular}
\end{table}

Six of the seven checks failed, but they do not describe six problems. They
collapse into two defects.

\subsubsection*{The double dispatch race}

\code{pcb\_mutex} fails at depth 901 when \code{executing[]} reaches 2. After 
checking the counterexample against \code{manager.c}, the interleaving is as 
follows. Worker A dequeues process $p$ and runs an instruction requesting a 
resource held by another process. \code{request\_resource} then sets $p$ to 
\code{WAITING} and adds it to \code{waitingq}. A releases \code{i\_lock} and 
is about to check \code{running\_p->state == WAITING} to decide whether to 
leave the loop. Before this happens, worker B releases the resource, causing 
\code{release\_resource} to move $p$ back to \code{readyq} and set its state to 
\code{READY}. B then dequeues $p$ and starts running it. A reads the updated 
\code{READY} state, does not leave the loop, and continues running $p$ as well.

\code{waiting\_consistent} fails 61 steps later in the same part of the execution. 
The trail shows a PCB in the \code{WAITING} state even though the resource it 
was waiting for has already been released by the other worker. Since 
\code{release\_resource} only wakes a waiting process when the resource
matches the one it is waiting for, this PCB remains in \code{waitingq} with 
no way to return to the ready queue. It also continues holding a resource 
that it will never release.

The deadlock run is the same defect from the consequence side. Its final state
has \code{instr\_count[2]} at 4 with \code{MAX\_INSTR} at 3, leaving a worker
stuck at the selection on \code{instr\_count} because neither guard is
executable. In \code{manager.c} that is two threads both running
\code{running\_p->next\_instruction = running\_p->next\_instruction->next} for
the same PCB, so the second advances a pointer already at the end of the list
and dereferences past it. C defines no behaviour there, and a Promela control
point with no executable transition is a state with no defined successor, so
the abstraction has nothing defined exactly where the implementation has
nothing defined.

The fix is to hold a lock across the state test and the branch that follows
it, not only across \code{execute\_instr}. Doing that with \code{i\_lock}
costs parallelism, since \code{i\_lock} already serialises every instruction
in the system; a per-PCB lock is the cheaper version, because then only
threads touching the same PCB serialise.

\subsubsection*{Self deadlock in \code{request\_resource}}

This counterexample is much smaller than the others, failing at depth 379 
after 188 states. The trail only contains one worker, and \code{executing[]} 
never becomes greater than 1, so there is no thread interleaving involved. 
This is simply a logic error that a single thread can reach on its own. The 
fix is to add a check before the availability test so that 
\code{resource->allocated == cur\_pcb} is treated as a successful request. 
The same \code{!resource\_found} branch is also used when the requested 
resource does not exist. These are two different cases, so the fix should 
distinguish between a resource already owned by the process, a resource held 
by another process, and a resource that does not exist.

This defect also explains both liveness failures and has another consequence 
that is not checked by the model. \code{detect\_deadlock} searches for cycles 
in the waiting queue, and a process waiting for a resource that it already 
owns creates a cycle of length 1. The program would therefore report a 
deadlock even though it is not the type of deadlock the diagnostic is 
intended to detect. A single missing check therefore causes a safety 
violation, 2 liveness violations, and an incorrect deadlock diagnosis.

\subsubsection*{Liveness and fairness}

Both liveness properties also fail without \code{-f}, at depth 14 after 25
states, and neither of those results is evidence of anything. The cycle they
report is one in which \code{init} evaluates its loop guard and is never
scheduled again, so \code{ready\_q} is never seeded and a worker spins on an
empty queue forever. No OpenMP runtime passes over a thread that can make
progress indefinitely \cite{openmp52}, and weak fairness excludes exactly
those cycles \cite{inggs_properties}, so every liveness result reported here
uses \code{-f}. Fairness is not free: \code{ready\_dispatched} finishes in 25
states without it and explores 11\,372\,991 with it.

The fair \code{all\_terminate} counterexample ends with all three processes
\code{WAITING}, process 1 holding resource 1 while waiting for resource 1, and
both workers already exited because \code{check\_terminate} counts every
process as blocked. \code{ready\_dispatched} fails for the same reason but
exposes a separate design consequence; which is the stopping condition retires the
workers as soon as everything is blocked, so a process can still be on the
ready path when the system decides it is finished.

\subsubsection*{The property that holds}

\code{no\_dup\_ready} was checked exhaustively at \code{MAX\_INSTR 2} with 0
errors over 20\,861\,451 states in about 21 seconds, using 2\,421\,MB. The run
also reported 0 of 12 states unreached in \code{init} and 0 of 119 unreached
in \code{Worker}, which is what makes the pass worth quoting: every control
point was visited, so the result is not an artefact of dead code in the 
abstraction \cite{inggs_spin}.

The reduced bound is necessary because of the size of the state space. Failed 
properties are relatively cheap to check because the search stops when the 
first counterexample is found. However, a property that passes must explore 
all reachable states. The state space grows by about a factor of 50 for each 
additional instruction. 420,705 states at \code{MAX\_INSTR\ 1} and 
20\,861\,451 at 2, which would result in roughly $10^9$ states at 3. 
With \code{pan} reporting 96 bytes per state, this would require around 
100\,GB of memory, compared to the 7\,GB available.

The bound of 2 is reasonable because the shortest possible duplicate requires 
a PCB to be added to the queue twice before it is removed once. This requires 
one blocking request followed by a wakeup, so two instructions are enough to 
produce the smallest possible counterexample. Increasing the bound would 
therefore mainly allow longer versions of the same behaviour. This argument is 
based on the shortest witness rather than a formal proof.

It is also important to note that the model uses a channel for the ready queue. 
A channel cannot contain two references to the same PCB in the way that an 
incorrectly maintained linked list could; this result checks the 
enqueue and dequeue behaviour rather than the pointer manipulation performed 
by \code{enqueue\_pcb}.


% =====================================================================
\section{Program 2: MPI Master/Worker Othello Engine}

% ---------------------------------------------------------------------
\subsection{The Promela Model}

The model abstracts the MPI master and worker move evaluation in
\code{my\_player.c}: \code{run\_master}, \code{execute\_master},
\code{send\_init\_moves}, \code{send\_next\_move}, \code{pop\_move} and
\code{run\_worker}. The referee is modelled as an environment process,
\code{Referee}, because a Promela model must be closed and the
communication flow through \code{comms.c} is fixed by the framework rather
than by us. The board, the search recursion, \code{evaluate} and the
Othello rules are all abstracted away. What survives of
\code{minimax\_pruning} is only which of three outcomes a search produces,
which is the least the properties in Section~\ref{sec:props-2} need.

Three constants control the size of the model, each using a \code{\#ifndef} 
guard so that it can be set from the command line. \code{NUM\_WORKERS} is set 
to 3 because \code{common/configs.py} in the Othello project uses 
\code{"threads": 4}. Since \code{comm\_sz} is 4, this gives one master and 
three workers, matching the actual system.

\code{MAX\_MOVES} must be greater than \code{NUM\_WORKERS}. Otherwise, 
\code{send\_init\_moves} can empty the move stack before any worker returns a 
result, meaning the dynamic work redistribution branch is never reached. This 
was discovered using \code{pan}'s unreached statement report with 
\code{NUM\_WORKERS 3, MAX\_MOVES 3}, which showed that the redistribution send 
was unreachable. \code{MAX\_MOVES 4} is therefore the smallest suitable value.
Finally, \code{MAX\_ROUNDS} limits the number of rounds in the \code{Referee} 
process so that each run terminates. It is set to 2 by default and varied 
between runs in Section~\ref{sec:results-2}.

Table~\ref{tab:mapping2} sets out thef
correspondence between the implementation and the model.

\begin{table}[htbp]
\centering
\caption{Mapping between the implementation and the model.}
\label{tab:mapping2}
\begin{tabular}{p{3.6cm} p{3.4cm} p{7cm}}
\toprule
\textbf{\code{my\_player.c}} & \textbf{Model} & \textbf{Reason} \\
\midrule
\code{MPI\_Bcast(\&my\_colour)}, \code{MPI\_Bcast(board)} &
\code{bcast\_colour[]}, \code{bcast\_board[]} &
For each collective there is 1 channel per worker. The board carries no
information any property needs, so it is reduced to the round number. \\
\addlinespace
\code{MPI\_Send}/\code{MPI\_Recv} with \code{MOVE\_TAG}, \code{NO\_WORK\_TAG} &
\code{to\_worker[]}, one buffered channel per worker &
Direct: the tag distinguishes work from termination exactly as in the
implementation. \\
\addlinespace
\code{MPI\_Iprobe(MPI\_ANY\_SOURCE)} \\ + \code{MPI\_Recv} &
\code{to\_master}, one shared channel &
\code{MPI\_ANY\_SOURCE} does not distinguish senders, so a single channel
every worker writes into is a direct abstraction, not a simplification. \\
\addlinespace
\code{move\_stack} (linked list) &
\code{moves\_left} counter &
Nothing else reads the stack and no other process touches it, so only its
size can affect any interleaving. \\
\addlinespace
\code{minimax\_pruning} &
\code{evaluate\_move} inline &
The board, recursion and \code{evaluate} are removed. What survives is
which of three outcomes a call produces, discussed below. \\
\addlinespace
\code{run\_master}'s if-chain over \code{comms.h} message types &
\code{Master}'s \code{if} over \code{mtype} &
Direct: the same branches are handled, and the same two are not. \\
\addlinespace
The Ingenious Framework referee (reached over a socket via \code{comms.c}) &
\code{Referee}, an environment process &
Promela models must be closed \cite{inggs_promela}; \code{budget} bounds
the messages so every run terminates. \\
\bottomrule
\end{tabular}
\end{table}

The headline abstraction is \code{evaluate\_move} (stands for the call
a worker makes into \code{minimax\_pruning}) and through it, the minimising
branch of \code{minimax}. That branch initialises an accumulator to
\code{INT\_MAX} and loops over the legal moves at that node:

\begin{lstlisting}[style=clang,caption={The minimising branch of \code{minimax}}]
minEval = INT_MAX;
legal_moves(moves, &number_of_moves, my_colour);
for (int i = 0; i < number_of_moves; i++)
{
  ...
  minEval = minimum(minEval, eval);
  beta = minimum(beta, eval);
  if (beta <= *alpha)
  {
    break;
  }
}
return minEval;
\end{lstlisting}

Three outcomes fall out of this; namely the loop never runs because there are no
legal moves at that node, so the function returns its initial value
unchanged; the loop runs once and stops, because the pruning test sits at
the bottom rather than the top, so a poisoned or reduced-depth call returns
its first child's value rather than a true minimum; or the loop runs to
completion and returns a genuine evaluation. The model reproduces exactly
these three outcomes as 3 branches of 1 \code{if}, resolved
non-deterministically wherever more than one applies:

\begin{lstlisting}[style=promela,caption={\code{evaluate\_move}, the abstraction of \code{minimax\_pruning}}]
inline evaluate_move(m,in_alpha,in_depth,out_score,out_alpha)
{
	if
	:: out_score = NO_EVAL
	:: (in_alpha == NO_EVAL || in_depth == DEPTH_REDUCED) ->
		select(out_score : 1 .. SCORE_MAX)
	:: (in_alpha != NO_EVAL && in_depth == DEPTH_FULL) ->
		out_score = true_score[m]
	fi;
	if
	:: out_score > in_alpha -> out_alpha = out_score
	:: out_score <= in_alpha -> out_alpha = in_alpha
	fi
}
\end{lstlisting}

\code{NO\_EVAL} represents \code{INT\_MAX} and is set to 9, which is larger 
than any valid score in the abstract domain \code{1..SCORE\_MAX}. This gives it 
the same behaviour as \code{INT\_MAX} for the comparisons made by the master. 
\code{true\_score[]} is only used for verification and has no equivalent in 
the implementation. At the start of each round, it assigns a non-deterministic 
valid score to each move. This allows the model to compare the move selected 
by the master with the move that should have been selected. The actual 
implementation does not have such an array because the correct value of a 
move is only known from a complete search.

Two omissions in Table~\ref{tab:omissions2} are not free, and both are
central to Section~\ref{sec:results-2}.

\begin{table}[htbp]
\centering
\caption{What the model leaves out, and what each omission costs.}
\label{tab:omissions2}
\begin{tabular}{p{4.4cm} p{9.6cm}}
\toprule
\textbf{Omitted} & \textbf{Cost} \\
\midrule
Board contents, geometry, \code{is\_stable}, board weights &
None $\rightarrow$ No property depends on where a piece sits, only on the score a search returns for it. \\
\addlinespace
Recursion inside \code{minimax} below the top call &
None $\rightarrow$ It is thread-local and cannot affect any other process's
interleaving. \\
\addlinespace
The \code{num\_moves}-to-depth mapping (11, 12, 13 or 14) &
None $\rightarrow$ Nothing in the protocol depends on the specific depth, only on
whether a search was cut short, so all four collapse to
\code{DEPTH\_FULL} and \code{DEPTH\_REDUCED}. \\
\addlinespace
The \code{time()} cutoff clock &
Small $\rightarrow$ A free nondeterministic choice can fire the cutoff at any point in
a round, which over-approximates a real clock rather than modelling one;
a counter would multiply the state space for no benefit to the properties
checked. \\
\addlinespace
The \code{MPI\_Iprobe} busy-wait, replaced by a blocking receive &
Not free $\rightarrow$ This changes what an unanswered result looks like to Spin,
discussed in Section~\ref{sec:limits-2}. \\
\bottomrule
\end{tabular}
\end{table}

One change was needed because of Promela's \code{select} syntax. 
\code{select} cannot assign directly to an array element, so 
\code{true\_score[i]} cannot be used as the target. Instead, the model selects 
a value into a temporary variable and then assigns it to the array. One other 
detail is that \code{bcast\_terminate} only sends the colour broadcast with a 
negative value and does not send the board. This is because the workers exit 
their loop before reaching the board receive, so the collective communication 
still matches on both sides.

% Here
\subsubsection*{Limits on interpretation}
\label{sec:limits-2}

As with Program 1, a counterexample is treated as a possible defect until it 
has been checked against the source code. Similarly, a property that passes 
only applies to the number of workers, moves, and rounds used in the model. 
There is also a limitation specific to this program. The implementation uses 
the \code{MPI\_Iprobe} busy-wait in \code{execute\_master} and the 
\code{while (running)} loop in \code{run\_master}. In the model, both are 
replaced with blocking channel receives. This changes how Spin detects the 
same underlying problem. If an \code{MPI\_Iprobe} loop never receives a 
message, the implementation can run indefinitely, which Spin would detect as 
a non-progress cycle using \code{-l}. In the model, a blocking receive on a 
channel that never receives a message results in an invalid end state, which 
is detected by the default safety check. In both cases, the master never 
returns; so the abstraction represents an implementation livelock as a model 
deadlock, making it easier to detect and producing a shorter counterexample. 
The results in Section~\ref{sec:results-2} are interpreted with this 
difference in mind rather than as an exact match to the C control flow.

% ---------------------------------------------------------------------
\subsection{Correctness Properties}
\label{sec:props-2}

Three variables have no counterpart in \code{my\_player.c} and exist only
so a property can be stated. This is the same role \code{executing[]} and
\code{in\_ready[]} play in the scheduler model. \code{round\_done} is false
while a round is in progress and true once the collection loop has
finished and \code{best\_move} is final. \code{best\_true} is the largest
\code{true\_score} among the round's moves. \code{workers\_done} counts how
many workers have left their loop and can reach \code{MPI\_Finalize}.

Five checks were run: four properties (as named \code{ltl} blocks), plus a
deadlock check that is not an LTL property. Table~\ref{tab:props2} gives
each formula as written in \code{model.pml}.

\begin{table}[htbp]
\centering
\caption{The 4 LTL properties as written in \code{model.pml}.}
\label{tab:props2}
\begin{tabular}{l l p{9.2cm}}
\toprule
\textbf{Property} & \textbf{Class} & \textbf{Formula} \\
\midrule
\code{no\_eval\_chosen} & safety &
\code{[] !(round\_done \&\& best\_score == NO\_EVAL)} \\
\addlinespace
\code{best\_is\_maximal} & safety &
\code{[] ((round\_done \&\& num\_moves > 0) -> true\_score[best\_move] == best\_true)} \\
\addlinespace
\code{no\_lost\_results} & safety &
\code{[] (round\_done -> len(to\_master) == 0)} \\
\addlinespace
\code{workers\_finish} & liveness &
\code{<> (workers\_done == NUM\_WORKERS)} \\
\bottomrule
\end{tabular}
\end{table}

\noindent
\textbf{\code{no\_eval\_chosen}} targets the leaf case in the minimising 
branch of \code{minimax}. There is no guard for \code{number\_of\_moves == 0}, 
so a node with no legal moves returns \code{minEval} with its initial value 
of \code{INT\_MAX}. This value is then used by \code{execute\_master} in the 
comparison \code{if ((process\_score > max\_score) || (max\_move == -1))}. 
Since \code{INT\_MAX} is larger than every valid evaluation, the move that 
was never searched can be selected; this affects the correctness of the move 
chosen by the engine, rather than just its internal state.

\medskip\noindent
\textbf{\code{best\_is\_maximal}} checks whether the move selected by 
\code{execute\_master} is actually one of the best available moves. 
The condition \code{num\_moves > 0} excludes the pass case, where 
\code{best\_move} is 0 and \code{true\_score[0]} has no meaning. Two 
issues can cause this property to fail. The first is the \code{NO\_EVAL} 
problem described above. The second is that the master compares scores 
calculated under different conditions. The search depth can starts at 11, or 12 to 14 when there are few legal moves, and drops to 4 when the turn clock passes \code{cut\_off}, while the shared 
\code{alpha} can cause some workers to prune earlier than others. Since the 
pruning check \code{if (beta <= *alpha) break;} is at the bottom of the loop, 
a depth-reduced or high-alpha search still evaluates its first child and can 
return a normal-looking score instead of \code{INT\_MAX} (this makes the 
second issue harder to detect than the first). The fault injection results 
separating these two causes are given in Section~\ref{sec:results-2}.

\medskip\noindent
\textbf{\code{no\_lost\_results}} is the property the whole work-sharing
protocol rests on, and the one expected to hold. \code{to\_master} stands
for the \code{MPI\_Iprobe} plus \code{MPI\_Recv(MPI\_ANY\_SOURCE)} pair, and
a single shared channel is the right abstraction because
\code{MPI\_ANY\_SOURCE} does not differentiate senders. The
\code{Iprobe} loop has no escape: the time cutoff lowers \code{depth} but
never breaks it, so if the send and receive counts were ever unbalanced the
master would spin forever. This property confirms the balance is exact.

\medskip\noindent
\textbf{\code{workers\_finish}} checks whether every worker eventually leaves 
its loop and reaches \code{MPI\_Finalize}. A worker only exits when it 
receives a negative broadcast colour, and only the \code{GAME\_TERMINATION} 
and \code{UNKNOWN} branches of \code{run\_master} send this broadcast. 
However, \code{receive\_message} in \code{comms.c} can also return 
\code{RECV\_FAILED} or \code{CLIENT\_DISCONNECTED}, and \code{run\_master} 
has no branch for either case. Since \code{comms.h} was fixed by the project 
specification and couldn't be changed, handling these values is the 
responsibility of the player. This failure occurs both with and without 
\code{-f}, confirming that it is caused by the missing branches rather than 
scheduling.

\medskip\noindent
\textbf{Deadlock} is checked in the same way as Program 1, using a 
claim-free verifier compiled with \code{-DNOCLAIM}. This is necessary 
because \code{pan} disables the invalid end state check when a never claim 
is active \cite{spin_pan}. The check looks for a reachable state where a 
process is blocked with no executable statement while the system has stopped 
making progress. It's useful to check this alongside \code{workers\_finish} 
because they identify the same problem in different ways, which is, one shows 
that the workers never finish, while the other shows that the system has 
become stuck.

\medskip\noindent
Two things were considered and not verified. A property comparing the
round number a worker receives against \code{round\_no} was drafted to
check that the two broadcasts, \code{bcast\_colour} and \code{bcast\_board},
stay matched. They are deliberately kept as two channels rather than
merged into one so that such a mismatch would be observable, and this
remains a natural extension rather than a checked result. Alpha
monotonicity within a round, that \code{master\_alpha} never decreases, was
also considered and dropped. It is true by construction in both the model
and the implementation, since \code{alpha} is only ever raised by a
comparison, therefore a property asserting it would hold trivially and establish
nothing.

% ---------------------------------------------------------------------
\subsection{Verification Results and Analysis}
\label{sec:results-2}

Each run selects a claim using \code{./pan -a -N name}. 
The deadlock check uses a second binary compiled with \code{-DNOCLAIM} from 
the same generated \code{pan.c}, as in Program 1 \cite{spin_pan}. 
\code{NUM\_WORKERS}, \code{MAX\_MOVES}, and \code{MAX\_ROUNDS} are supplied to
\code{spin} on the command line. Both binaries are rebuilt after each 
\code{spin -a} to ensure that the results match the current configuration.

All runs were performed using Spin 6.5.2 on the same machine with 7\,GB of 
RAM used for Program 1 and iSpin was again not used.

\begin{table}[htbp]
\centering
\caption{Verification results. Depths and state counts as
reported by \code{pan}.}
\label{tab:results2}
\begin{tabular}{l l l r r}
\toprule
\textbf{Property} & \textbf{Config} & \textbf{Result} & \textbf{Depth} & \textbf{States} \\
\midrule
\code{no\_eval\_chosen} & \code{W3, M4, R1} & violated & 205 & 5\,336 \\
\code{best\_is\_maximal} & \code{W3, M4, R1} & violated & 255 & 106\,138 \\
\code{workers\_finish} & \code{W3, M4, R1} & violated & 31 & 203 \\
deadlock & \code{W3, M4, R1}, \code{-DNOCLAIM} & violated & 20 & 205 \\
\code{no\_eval\_chosen} & \code{W2, M3, R1} & violated & 172 & 1\,046 \\
\code{best\_is\_maximal} & \code{W2, M3, R1} & violated & 222 & 17\,286 \\
\code{workers\_finish} & \code{W2, M3, R1} & violated & 28 & 77 \\
deadlock & \code{W2, M3, R1}, \code{-DNOCLAIM} & violated & 17 & 79 \\
\code{no\_lost\_results} & \code{W2, M3, R1} & holds & n/a & 7\,676\,089 \\
\code{no\_lost\_results} & \code{W3, M4, R1}, \code{-DBITSTATE} & 0 errors, approximate & 436 & 655\,692\,780 \\
\bottomrule
\end{tabular}
\end{table}

\code{W}, \code{M} and \code{R} abbreviate \code{NUM\_WORKERS},
\code{MAX\_MOVES} and \code{MAX\_ROUNDS}. \code{W3, M4, R1} matches the
deployed system; \code{W2, M3, R1} is the reduced configuration, small
enough to search exhaustively, used wherever a property must hold rather
than merely fail fast.

\subsubsection*{The \code{NO\_EVAL} leak}

\code{no\_eval\_chosen} fails at depth 205 when a single worker reaches a node 
with no legal moves. As with \code{no\_self\_wait} in Program 1, this is a 
simple logic error rather than a race condition. The problem is also not 
limited to the first round. With \code{MAX\_ROUNDS 2}, the property fails 
again at depth 341 after 5,683 states, after the first round has already 
finished. The match logs in \code{othello/evidence/} also show that this 
occurs in practice. In match \code{20250513\_115728}, 22 of 60 rounds ended 
with a score of 2147483647, and workers received an \code{alpha} value of that value
76 times during the same match.


\subsubsection*{Two independent causes of a suboptimal move}

\code{best\_is\_maximal} fails at depth 255, saying the engine played a
worse move than one it had already evaluated, the same shape as round 47
vs. round 24 in the match log, where move 47 scored 1328 and move 24
scored \code{INT\_MAX} and was played anyway. Section~\ref{sec:props-2}
gives two mechanisms that can cause this on their own, the fault injection
runs in Table~\ref{tab:fault} isolate them.

\begin{table}[htbp]
\centering
\caption{Fault isolation on \code{best\_is\_maximal}, \code{W2, M3, R1}.}
\label{tab:fault}
\begin{tabular}{l l r r}
\toprule
\textbf{Flags} & \textbf{Result} & \textbf{Depth} & \textbf{States} \\
\midrule
none & violated & 222 & 17\,286 \\
\code{-DNO\_TIME\_CUTOFF} & violated & 218 & 10\,026 \\
\code{-DNO\_LEAK} & violated & 280 & 47\,056 \\
\code{-DNO\_TIME\_CUTOFF -DNO\_LEAK} & holds & n/a & 626\,037 \\
\bottomrule
\end{tabular}
\end{table}

Removing either cause on its own still leaves the property violated; 
the property only holds when both causes are removed. This shows that the 
two problems are independent. The \code{NO\_EVAL} leak can cause a suboptimal 
move by itself, and comparing scores calculated at different depths or with 
different alpha values can also cause a failure. So, fixing only the 
\code{minimax} leak would not be enough to make the property hold.

\subsubsection*{The missing message-type branch}

\code{workers\_finish} fails at depth 31, while the deadlock check finds an
invalid end state at depth 20 in the same configuration. Both failures are 
caused by the missing \code{RECV\_FAILED} and \code{CLIENT\_DISCONNECTED} 
branches in \code{run\_master}. The smallest counterexample does not require 
a move round. With \code{MAX\_ROUNDS 0}, the deadlock still occurs at depth 
20 after 205 states, while \code{workers\_finish} fails at depth 31 after 203 
states. This shows that the defect is entirely within the referee message 
loop and is unrelated to move evaluation.

In contrast to the liveness properties in Program 1, \code{workers\_finish} fails 
both with and without \code{-f}. The problem is a missing branch rather than 
a scheduling issue that can be addressed through fairness. Therefore, no 
fairness argument is needed here, unlike for \code{all\_terminate} and 
\code{ready\_dispatched}.

\subsubsection*{The property that probably holds}

\code{no\_lost\_results} was checked exhaustively with \code{W2, M3, R1}, 
finding 0 errors across 7\,676\,089 states in 11.4 seconds. All 4 proctypes 
also had 0 unreached statements. At the deployed configuration, 
\code{W3, M4, R1}, the exhaustive search was stopped by the kernel's 
out-of-memory killer. The state vector is 244 bytes, so 7\,GB can store 
roughly $2.6 \times 10^7$ states, and the search exceeded this before 
finishing. As a fallback, bitstate storage was used. This stores states in a 
hash-based bit array, meaning different states can map to the same entry and 
be treated as already visited. The run found 0 errors across 655\,692\,780 
states in about 1,370 seconds. However, its hash factor of 6.55 is well 
below 100, which indicates that the search was not close to exhaustive. 
Therefore, the result only shows that no counterexample was found in the 
approximate search of roughly $6.6 \times 10^8$ states. The exhaustive 
two-worker result provides stronger evidence, so both results are reported.

The reduction to two workers is reasonabel for this property. A lost result 
requires the master to finish a round while a result is still waiting in the 
queue. The shortest example only needs one worker whose result is processed 
and another whose result is missed, which can occur with two workers. 
Adding a third worker would mainly allow longer versions of the same 
behaviour. This is an argument based on the shortest counterexample rather 
than a proof, so the result only applies within the verified bound.

One search-order detail is important because it affects the reported 
state counts without changing their meaning. The 4 terminal message branches 
in \code{proctype Referee} are placed before the \code{budget > 0} branch. 
Since depth first search explores the nondeterministic \code{do} branches in 
the order they appear, this allows a termination counterexample to be found 
after only a few hundred states, rather than after exploring a full move 
round. This explains the much larger state counts seen in earlier versions 
of the model. The reordering only changes the order in which states are 
explored, not the state space itself. For example, \code{no\_lost\_results} 
at \code{W2, M3, R1} reports exactly 7\,676\,089 states both before and after 
the reordering because the property must be checked exhaustively and 
therefore visits every reachable state.

\subsubsection*{Proposed fixes}

Each of the three defects needs a different kind of fix.

\begin{itemize}
  \item The \code{NO\_EVAL} leak is the easiest to fix. The minimising branch 
  of \code{minimax} needs a guard for \code{number\_of\_moves == 0} before the 
  loop, since a node with no legal moves is a normal pass in Othello, not an 
  error. It should return a real evaluation of the current position using 
  \code{evaluate}, rather than the \code{INT\_MAX} value used to initialise 
  the accumulator. The maximising branch has the same issue with 
  \code{INT\_MIN}, so both branches should include the guard even though only 
  the minimising case was found here. As an additional safeguard, 
  \code{execute\_master} could reject a score equal to \code{INT\_MAX} 
  before comparing it with \code{max\_score}, preventing a leaked invalid 
  score from reaching the shared \code{alpha}.
  \item The comparability defect requires a design change rather than just an 
  extra test. The master lowers \code{depth} from 11-4 during a round, then 
  compares scores from both depths as if they were on the same scale. 
  The simplest fix is to apply the new depth from the next round, so that all 
  scores in a round use the same search depth. If the turn clock cannot wait, 
  another option is to record the depth used for each result and only keep 
  the best move from the deepest completed depth, instead of mixing scores 
  from different depths. Table~\ref{tab:fault} shows why this is needed, 
  since fixing the leak alone still leaves \code{best\_is\_maximal} violated.
  \item The missing message branch is the smallest fix of the three, and the safer
  form of it is not to add 2 more \code{else if} arms for
  \code{RECV\_FAILED} and \code{CLIENT\_DISCONNECTED}, but to give the
  if-chain a final \code{else} that sets \code{running = 0} and issues the
  termination broadcast. Naming the two values explicitly fixes today's defect;
  a default arm also covers any value \code{comms.h} might gain later, which
  matters because the interface is fixed by the framework and the player cannot
  change it. Either way the workers stop blocking in \code{MPI\_Bcast} and
  reach \code{MPI\_Finalize}, which is what \code{workers\_finish} asks for.
\end{itemize}

% =====================================================================
\appendix
\section{Reproducing the verification results}

\begin{table}[htbp]
\centering
\caption{Environment for every run reported in this document.}
\label{tab:env}
\begin{tabular}{l l}
\toprule
Model checker & Spin Version 6.5.2, 6 December 2019 \\
Compiler & gcc \\
Platform & Ubuntu Linux \\
Memory & 7\,GB RAM, 1\,GB swap \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:env} gives the environment. iSpin was not used for any result
in either program, and all runs were performed from the command line.

\subsection*{Program 1: FCFS scheduler}

Model version: commit \code{517e9be}.

\begin{lstlisting}[language=bash,caption={Build}]
cd Modelchecking/scheduler
spin -a model.pml
gcc -O2 -o pan pan.c
gcc -O2 -DNOCLAIM -o pan_noclaim pan.c
\end{lstlisting}

\begin{lstlisting}[language=bash,caption={Verification runs, default configuration (MAX\_INSTR 3)}]
./pan -a -N pcb_mutex
./pan -a -N no_self_wait
./pan -a -N waiting_consistent
./pan -a -N all_terminate -m50000
./pan -a -N all_terminate -f -m50000
./pan -a -N ready_dispatched -m50000
./pan -a -N ready_dispatched -f -m50000 -w26
./pan_noclaim
\end{lstlisting}

\begin{lstlisting}[language=bash,caption={Reduced configuration (MAX\_INSTR 2), for the property that holds}]
spin -a -DMAX_INSTR=2 model.pml
gcc -O2 -o pan pan.c
./pan -a -N no_dup_ready -m20000 -w26
\end{lstlisting}

\subsection*{Program 2: Othello}

Model version: commit \code{70b73d2}.

Every result in Table~\ref{tab:results2} and Table~\ref{tab:fault}, except
the three-worker \code{no\_lost\_results} run below, is produced by a single
script:

\begin{lstlisting}[language=bash,caption={Build and run everything}]
cd Modelchecking/othello
./verify.sh
\end{lstlisting}

This regenerates \code{pan.c} and rebuilds both binaries before each group
of runs, writes every error trail to \code{results/}, the full \code{pan}
output of every run to \code{results/logs/}, and a summary table to
\code{results/summary.txt}. Every run is given a wall clock limit of one
hour. Passing \code{quick} as the first argument skips the two
\code{no\_lost\_results} runs: the three-worker one, which exhausts
memory, and the two-worker one, which at 11.4 seconds is the longest run
that completes.

The three-worker \code{no\_lost\_results} run is attempted by the script
but is not expected to finish. It is the run the out-of-memory killer
stops, and it appears in \code{summary.txt} as \code{INCOMPLETE} with
coverage \code{unknown} rather than as a result. Its bitstate fallback is
not part of the script, because it takes roughly 23 minutes and is a
deliberate one-off rather than a routine result, so it is run manually:

\begin{lstlisting}[language=bash,caption={Three-worker \code{no\_lost\_results}, bitstate fallback}]
spin -DNUM_WORKERS=3 -DMAX_MOVES=4 -DMAX_ROUNDS=1 -a model.pml
gcc -O2 -DBITSTATE -o pan_bit pan.c
./pan_bit -a -N no_lost_results -m100000 -w32
\end{lstlisting}

\section*{AI Declaration}
Generative AI was used in an assisting role throughout the project. 

For the code, it was used to provide guidance when encountering difficulties and
assist in identifying the cause of issues.

For the report, generative AI was used to provide guidance on the overall 
structure, improve grammar and sentence construction, and suggest clearer 
wording for technical explanations. The final content, technical decisions, 
analysis, results, and conclusions were my own work.

\bibliographystyle{plainnat}
\bibliography{refs}

\end{document}